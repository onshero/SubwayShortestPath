package util;
import control.LineManager;
import control.StationManager;

public class MyUtil {
	public static LineManager lineManager = new LineManager();
	public static StationManager stationManager = new StationManager();
}
